# Development / test dependencies.
#   pip install -r requirements-dev.txt
-r requirements.txt
pytest>=8.0
pytest-qt>=4.4
# Local WebDAV server for the sync integration test.
wsgidav>=4.0
cheroot>=10.0
